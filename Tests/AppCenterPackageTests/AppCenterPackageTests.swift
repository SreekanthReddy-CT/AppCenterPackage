import XCTest
@testable import AppCenterPackage

final class AppCenterPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AppCenterPackage().text, "Hello, World!")
    }
}
